# BEP AI Agent 知识架构与训练方案 (Implementation Plan)

基于我们刚构建的知识库，以及您 `myAI` 系统底层的多 Agent、云端知识注入 (`loadCloudKnowledge`)、XML 标签动作触发机制，我为您设计了以下**“三层知识漏斗”**的 AI 训练与配置方案。

## 核心设计理念
不要让单个 AI 背负所有知识！避免 Context Window 过载和答非所问。必须利用 `myAI` 的机制做到**“按需加载，分层调用”**。

---

## 🏗️ 知识分层与 Agent 训练策略

### 💡 第 1 层：核心知识 + 路由字典 (Core Knowledge & Routing)
**定位**：AI 的“本能”与“迎宾/接待”大脑。
**处理方式**：**强制全量注入系统提示词 (System Prompt Injection)**

- **对应文件 (系统提示词强制注入)**：
  - 核心字典：`Index_Dictionary.md` (控制 AI 的检索与转接逻辑)
  - 基础业务：`01_BEP_Core_Business.md` (核心理念与卖点)
  - 团队架构：`02_BEP_Organisational_Structure.md` (对接人职责)
  - 实战话术：`03_BEP_Common_QA.md` (打消学校顾虑的标准反驳)
  - 法务条款：`04_BEP_Agreements.md` (两种收费模式对比与防绕开条款)
- **在 `myAI` 中的实现**：
  - 这几个 Markdown 文件体积都在 `8KB` 限制内。将它们上传至 `AiAgentRepository` 并绑定给首面接待 Agent，`myAI` 的 `loadCloudKnowledge` 会自动将它们注入为 `## 已注入的知识库文件`。
  - **行为训练 (Prompt)**：赋予 AI **判定权**。在 `extra_prompt` 中明确规定：“当客户提出质疑时，立刻调用 QA 库中的话术反驳”；“当客户询问合作细节时，对比 Fixed Retainer 和 Performance 的区别”。
- **效果**：极速响应 90% 的基础和商业模式提问，无需调用任何额外 Tool。

### 📚 第 2 层：细节业务知识 (Detailed Context)
**定位**：AI 的“深度外脑”与实战记忆。
**处理方式**：**按需动态检索 (Queryable Cloud Files)**

- **对应文件 (作为云端挂载文档按需检索)**：
  - 进度大盘：`05_BEP_School_Communications.md` (19所学校进度与谈判难点汇总)
  - 原始沟通：`/School E-mail trail/` 目录下的 19 份真实往来邮件 PDF
  - 原始提案：`/BEP Docs & Presentation/Proposals/` 目录下的各个针对性提案原件
  - 宣传册源文件：`BEP Prospectus.pdf`, `BEP Presentation.pdf`
- **在 `myAI` 中的实现**：
  - 由于文件体积大且客户不一定会问到，只需将其挂载到云端。在 prompt 中它们会体现为 `## 可查询的云端文件` (如 `cloud://ID/05_BEP_School_Communications.md`)。
  - **行为训练 (Prompt & Tool)**：训练 AI **遇到不知情细节时主动使用查询工具**。例如设定规则：“当用户询问具体某个学校（如 DCSF, St Clare's）的进度时，你必须先调用 `dash_search` / `mcpTools.dashGenericGet` 工具检索云端文件获取最新进展，**绝对禁止自行编造进度**。”

### 🛠️ 第 3 层：文书样本与模板 (Templates & Actions)
**定位**：AI 的“手”与“生产力中心”。
**处理方式**：**隐式模板 + XML/Tool 动作触发 (Action Calling)**

- **对应资源 (仅供后端代码调用，不入 AI 提示词)**：
  - 销售邮件模板：根据不同谈判阶段（如初次触达、被拒绝后跟进、敲定会议）提取的标准邮件写法（待整理为代码/向量模板）。
  - 合同生成引擎：`/Agreements` 目录下的固定收费和绩效收费 Word 版合同草案，转换为服务端生成变量。
  - 定制化 Proposal 模板：用于在 AI 收集足学校信息（规模、目标市场、空缺床位）后，套用生成的文书系统。
- **在 `myAI` 中的实现 (结合您代码中 `<zj_report>` 的机制)**：
  - **切忌**将大段模板塞入 Prompt 让 AI 去死记硬背！普通的 AI 记不住长排版，且极易出错。
  - **行为训练 (Tool Definition)**：为 AI 开发专属的 Tool（例如 XML 标签触发器 `<generate_bep_email target_school="xxx" mode="fixed" />`，或者 MCP Tool `draft_document`）。
  - **执行逻辑**：AI 只需要理解“**现在需要给客户发邮件了**”，然后输出该 XML 标签/调用 Tool 并传入关键参数（目标学校、核心论点）。
  - **后端接管**：由 `myAI` 的服务端拦截该请求，服务端去读取真正的文书模板，将 AI 提供的信息填入模板中并返回。这就保证了输出格式的 100% 精准与专业。

---

## 🚦 多 Agent 转接路由 (Handoff Strategy)
结合您的 `buildHandoffGuide`，建议针对 BEP 设立至少两个 Persona：

1. **BEP 首席咨询顾问 (Frontend Agent)**：
   - 掌握第 1 层核心知识，擅长高情商沟通，回答 QA，打消客户顾虑。
2. **BEP 方案架构师/法务助理 (Backend Agent)**：
   - 掌握第 2 层细节与第 3 层模板。
   - 当客户说：“好，那针对我们学校的具体情况，你能出一份方案吗？”
   - **Frontend Agent 触发转接**：`[HANDOFF target="bep_architect" reason="方案细节和合同起草，我帮您转接我们的高级方案架构师"]`
   - **Backend Agent 介入**：读取聊天记录中的学校信息 -> 调取第 2 层了解该校特殊情况 -> 触发第 3 层 Tool 自动生成完美排版的 Proposal。

## User Review Required

> [!IMPORTANT]
> **确认您的 `myAI` 后端支持能力**
> 请确认您的后端 MCP Tools 中是否已经存在或准备开发一个专门用来“套用模板生成长文本”的 Tool？（类似现有的 `zj_report` 但专门用于邮件/合同起草）。确认后，我们可以立刻开始撰写这些专属的 Prompt 指令和 Tool Descriptions。
