# BEP AI Toolbox 完善计划：新增 Proposal 工具与知识注入

根据您的需求，目前 Toolbox 里的四大金刚“3缺一”（PPT、Webpage、Legal 已有），我们需要补齐核心的 **Proposal 写作工具**，并针对所有的生成工具进行**业务知识（BEP Core Business）与特定输出规制的深度训练注入**。

## User Review Required

> [!IMPORTANT]
> 请确认以下关于 Proposal 生成器的输入字段和底层调用的知识点是否符合您的预期？确认后我将开始创建前端界面、API 后端并将其接入到大模型管线中。

## Open Questions

> [!TIP]
> 目前知识库（Database）中存有 `BEP Introduction` 和 `Proposal Template` 等文件。
> 在后台生成 Proposal 时，我是**自动默认抓取这些系统级的 BEP 模板和公司介绍**塞给大模型作为核心 Context，还是全部依赖用户在前端用 `KbFileSelector` 手动选择？（建议：**系统自动强制注入核心 BEP 知识 + 用户按需补充特定学校邮件**）。

## Proposed Changes

我们将分为两部分进行：**新建 Proposal 工具** 和 **升级现有工具的知识浓度**。

### 1. 新增 Proposal 写作工具 (Frontend & Backend)

#### [NEW] `src/app/(dashboard)/toolbox/proposal/page.tsx`
- **目标**：提供专属的定制化 Proposal 表单前端。
- **输入字段设计**：
  - **目标学校** (Target School Name)
  - **学校情况/痛点** (School Profile & Needs - e.g. 招生不足、目标特定市场)
  - **商业模式偏好** (Fixed Retainer 还是 Performance Partnership)
  - **重点侧重** (Focus Areas - e.g. 学术、课外活动、寄宿体验)
  - **背景资料** (从知识库选择相关邮件往来、学校简介等)
- **UI 呈现**：左侧表单输入，右侧 Markdown 富文本预览（带复制/编辑功能）。

#### [NEW] `src/app/api/toolbox/proposal/route.ts`
- **目标**：大模型生成核心逻辑。
- **知识注入 (The 3-layer Funnel)**：
  - 后端在收到请求时，除了读取用户选中的特定附件，还将**自动从 Prisma 数据库检索或在系统提示词中内置 BEP 的核心话术与 `Proposal Template` 框架**。
  - 强制约束 AI 的输出排版必须符合 BEP Proposal 模板（包含 Executive Summary, Proposed Strategy, Target Markets, Commercial Terms 等模块）。

### 2. 完善系统布局与路由

#### [MODIFY] `src/app/(dashboard)/toolbox/layout.tsx`
- 在侧边栏导航 `TOOLS` 数组中增加 `Proposal 写作` (使用 `Briefcase` 图标和 `blue` 主题色)。

### 3. 增强其他工具的业务规制 (PPT & Webpage)

#### [MODIFY] `src/app/api/toolbox/ppt/route.ts` (及对应的 Webpage / Legal)
- **目标**：将 BEP 业务属性强化。
- **改动**：在 System Prompt 层面，赋予 AI “你代表 BEP” 的身份认知，并在处理关于留学、招生、学校推广主题时，自动带有 BEP 视角的专业度，避免生成空泛的通用内容。

## Verification Plan

### Automated / Manual Verification
1. 访问 `/toolbox/proposal` 检查 UI 渲染及侧边栏高亮是否正确。
2. 填入测试学校（如 Queen's College）和合作模式，点击生成。
3. 观察生成的 Proposal Markdown 是否严格遵循了商业模式，并且包含了 BEP 的核心卖点与正确的排版结构。
4. 测试现有工具（如 Legal）是否仍然稳定运行。
