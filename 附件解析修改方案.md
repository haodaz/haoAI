# 文件附件管线修复 — 实施计划

## 问题全景

从你的截图可以清晰看到问题链：用户上传 4 个文件（xlsx, pdf, docx, md），走完管线后 AI 输出的是**假设性内容**（"假设的附件1总结"、"假设为PDF格式"），说明 **Kelly 根本没有收到文件的实际内容**。

经过完整链路追踪，我定位了以下断点：

```mermaid
graph LR
    A["new-task 上传4个文件"] -->|"upload API 返回 attachments"| B["前端持有 attachments[]"]
    B -->|"setPendingDispatchTask({attachments})"| C["office 接收"]
    C -->|"❌ 解构时漏掉 attachments"| D["orchestrate API"]
    D -->|"❌ 没收到 attachments"| E["创建 TaskContext"]
    E -->|"attachments=null"| F["Kelly 读取 context.attachments"]
    F -->|"getTaskAttachments() → 空数组"| G["Kelly 没有文件内容"]
    G -->|"AI 猜测内容"| H["垃圾输出"]
```

---

## Issue 0: 任务历史不展示附件

### 根因

- [TaskContext](file:///Users/aisandbox/Documents/myAI/prisma/schema.prisma#L96-L110) 模型有 `attachments` 字段（JSON 字符串），数据已存入 DB
- 但 [history/page.tsx](file:///Users/aisandbox/Documents/myAI/src/app/(dashboard)/history/page.tsx) **完全没有读取和渲染 `attachments` 字段**（搜索确认 0 条匹配）
- 在「全自动执行」流程中 (`handleAutoDispatch`)，attachments 通过 `setPendingDispatchTask` 传到 office，但 office 的 `handleDispatch` 调用 `orchestrate` API 时**未传递 attachments**，导致 `TaskContext.attachments` 存为 null

### 修改方案

#### [MODIFY] [page.tsx](file:///Users/aisandbox/Documents/myAI/src/app/(dashboard)/office/page.tsx#L159-L171)

`useEffect` 解构 `pendingDispatchTask` 时，加入 `attachments`，并传给 `handleDispatch`：

```diff
  useEffect(() => {
    if (pendingDispatchTask) {
-     const { input, inputMode, contextId, tasks } = pendingDispatchTask;
+     const { input, inputMode, contextId, tasks, attachments } = pendingDispatchTask;
      setPendingDispatchTask(null);
      if (contextId && tasks) {
        handleDispatchWithTasks(input, tasks);
      } else {
-       handleDispatch(input, inputMode);
+       handleDispatch(input, inputMode, attachments);
      }
    }
  }, [pendingDispatchTask]);
```

#### [MODIFY] [page.tsx](file:///Users/aisandbox/Documents/myAI/src/app/(dashboard)/office/page.tsx#L173-L189)

`handleDispatch` 接收并转发 `attachments` 到 orchestrate API：

```diff
- const handleDispatch = async (dispatchInput: string, dispatchMode: string) => {
+ const handleDispatch = async (dispatchInput: string, dispatchMode: string, attachments?: any[]) => {
    ...
    body: JSON.stringify({
      source: 'TEXT',
      rawContent: dispatchInput,
      locale: i18n.language,
+     attachments,
    })
```

#### [MODIFY] [history/page.tsx](file:///Users/aisandbox/Documents/myAI/src/app/(dashboard)/history/page.tsx#L229-L233)

在「原始输入」区域下方添加附件展示区：

```tsx
{/* 附件列表 */}
{selectedCtx.attachments && (() => {
  try {
    const atts = JSON.parse(selectedCtx.attachments);
    if (!Array.isArray(atts) || atts.length === 0) return null;
    return (
      <div className="bg-blue-50/50 rounded-lg p-3 mt-2">
        <p className="text-[10px] font-bold text-blue-500 mb-2">📎 关联附件 ({atts.length})</p>
        <div className="flex flex-wrap gap-2">
          {atts.map((a: any) => (
            <span key={a.id} className="inline-flex items-center gap-1 px-2 py-1 bg-white border border-blue-200 rounded text-[10px] text-blue-800 font-medium">
              <FileText className="w-3 h-3 text-blue-400" />
              {a.originalName}
              <span className="text-blue-400">({(a.size/1024).toFixed(0)}KB)</span>
            </span>
          ))}
        </div>
      </div>
    );
  } catch { return null; }
})()}
```

---

## Issue 1: 多文件只拆一个 Kelly，无法并行处理

### 根因

当前 Chief 的分派逻辑是：看到有附件 → 分配一个 Kelly 任务 → 把所有 attachmentIds 都塞给这一个 Kelly。如你截图所示，4 个文件只产生了 1 个 Kelly 任务。

**问题本质**：Kelly 是 AI token 窗口，不是真人。4 个文件灌进一个 prompt 会：
- 超出 token 限制（尤其 xlsx 和 pdf 很长）
- 混淆不同文件的内容
- 无法单独总结每个文件

### 修改方案

分两层解决：**Chief prompt 引导** + **后端自动拆分**

#### [MODIFY] [agent_capabilities.yaml](file:///Users/aisandbox/Documents/myAI/public/characters/bristh_chief/agent_capabilities.yaml#L144-L155)

修改 Kelly 的 notes，明确指导 Chief 对多文件场景拆分任务：

```diff
  notes: "Kelly 是文件型任务的专属处理者。当 Chief 判断某个附件需要被「处理」（fileRole=target）而非「参考」时，必须分配给 Kelly。
- 她接收附件的 extractedText 并直接产出处理结果。"
+ 她接收附件的 extractedText 并直接产出处理结果。
+ 【多文件规则 — 极其重要】当有多个需要「处理」的附件时，你必须为每个附件创建一个独立的 Kelly 任务（每个任务的 attachmentIds 只包含一个文件ID）。
+ Kelly 不是真人，而是 AI token 窗口——多个 Kelly 任务会并行执行，效率更高、质量更好。
+ 只有当任务明确要求「跨文件对比/整合」时，才可以将多个附件放进同一个 Kelly 任务。"
```

#### [MODIFY] [orchestrate/route.ts](file:///Users/aisandbox/Documents/myAI/src/app/api/bristh/orchestrate/route.ts#L168-L186) 和 [orchestrate/analyze/route.ts](file:///Users/aisandbox/Documents/myAI/src/app/api/bristh/orchestrate/analyze/route.ts)

在 Chief AI 返回结果后，**后端做一层保底拆分逻辑**：如果某个 Kelly 任务的 `attachmentIds` 包含 2+ 个文件，且 `fileRole` 不是 `"cross-reference"`，自动克隆成多个并行 Kelly 任务。

```typescript
// 保底: 自动拆分 Kelly 多文件任务
const expandedTasks: any[] = [];
for (const t of tasksToCreate) {
  if (t.agent === 'Kelly' && t.attachmentIds?.length > 1 && t.fileRole !== 'cross-reference') {
    // 拆分成每个文件一个 Kelly 任务
    for (const attId of t.attachmentIds) {
      const att = attachments?.find((a: any) => a.id === attId);
      expandedTasks.push({
        ...t,
        attachmentIds: [attId],
        instruction: `${t.instruction}\n\n【处理文件】${att?.originalName || attId}`,
      });
    }
  } else {
    expandedTasks.push(t);
  }
}
tasksToCreate = expandedTasks;
```

> [!IMPORTANT]
> 同时需要修改 `orchestrate/analyze/route.ts` 的对应位置，因为两条路径（全自动 / 人工审核）都可能产出 Kelly 任务。

---

## Issue 2: 文件解析失败 — AI 收不到内容

### 根因

从截图看 Kelly 输出的是「假设性总结」，说明 `extractedText` 要么是空，要么解析错误。需要逐个排查 [file-parser.ts](file:///Users/aisandbox/Documents/myAI/src/lib/file-parser.ts)：

#### PDF (`pdf-parse` v2)

[file-parser.ts L77-95](file:///Users/aisandbox/Documents/myAI/src/lib/file-parser.ts#L77-L95)：代码使用了 `pdf-parse` v2 的 `PDFParse` 类 API，但 **`pdf-parse@2.4.5` 的实际 API 可能不同**。需要验证：

```typescript
// 当前写法 (可能不匹配)
const { PDFParse } = await import('pdf-parse');
const parser = new PDFParse({ data: buffer });
const result = await parser.getText();
```

**验证方法**：在 `node_modules/pdf-parse` 中检查实际导出 API。如果是 v2，可能需要：
```typescript
// pdf-parse v2 可能的正确用法
import PDFParser from 'pdf-parse';
const result = await PDFParser(buffer);
// result.text, result.numpages
```

#### DOCX (`mammoth`)

[file-parser.ts L100-111](file:///Users/aisandbox/Documents/myAI/src/lib/file-parser.ts#L100-L111)：代码正确，`mammoth.extractRawText({ buffer })` 是正确的 API。但需确认 `mammoth@1.12.1` 在 Next.js 服务端能正常 dynamic import。

#### XLSX (`xlsx`)

[file-parser.ts L201-236](file:///Users/aisandbox/Documents/myAI/src/lib/file-parser.ts#L201-L236)：代码看起来正确（`XLSX.read(buffer, { type: 'buffer' })`）。但 `xlsx@0.18.5` 是 SheetJS 社区版，需确认实际安装状态。

### 修改方案

#### [MODIFY] [file-parser.ts](file:///Users/aisandbox/Documents/myAI/src/lib/file-parser.ts#L77-L95) — 修复 PDF 解析

```typescript
async function parsePDF(buffer: Buffer): Promise<ParsedFile> {
  try {
    // pdf-parse 默认导出是一个函数 (不是 class)
    const pdfParse = (await import('pdf-parse')).default;
    const result = await pdfParse(buffer);
    return {
      extractedText: result.text || '[PDF 无文本内容]',
      pageCount: result.numpages,
    };
  } catch (err: any) {
    console.error('PDF parse error:', err.message);
    const fallbackText = extractPlaintextFromBuffer(buffer);
    if (fallbackText && fallbackText.length > 50) {
      return { extractedText: `[PDF 解析降级模式]\n${fallbackText}` };
    }
    return { extractedText: `[PDF 解析失败: ${err.message}]` };
  }
}
```

#### 验证步骤

在实施前，需要先运行验证脚本确认每个 parser 的实际 API：

```bash
# 1. 验证 pdf-parse 实际导出
node -e "const p = require('pdf-parse'); console.log(typeof p, typeof p.default, typeof p.PDFParse, Object.keys(p))"

# 2. 验证 mammoth
node -e "const m = require('mammoth'); console.log(typeof m.extractRawText)"

# 3. 验证 xlsx
node -e "const X = require('xlsx'); console.log(typeof X.read, typeof X.utils)"
```

根据验证结果修正 `file-parser.ts`。

> [!IMPORTANT]
> 这是最关键的问题。即使 Chief 正确分配了文件，如果 `extractedText` 为空或错误信息，Kelly 仍然没有内容可处理。必须先保证解析成功。

---

## Issue 3 (隐藏问题): 全自动流程附件丢失

### 根因

这是 Issue 0 中已识别的问题。完整链路如下：

1. `new-task` 页面 `handleAutoDispatch()` → `setPendingDispatchTask({ input, inputMode: 'text', attachments })` ✅ 附件已传
2. `office/page.tsx` useEffect 接收 → `const { input, inputMode, contextId, tasks } = pendingDispatchTask` ❌ **没解构 `attachments`**
3. `handleDispatch()` 调用 orchestrate → `body: JSON.stringify({ source, rawContent, locale })` ❌ **没传 `attachments`**
4. `orchestrate/route.ts` 接收 `attachments` → `TaskContext.create({ attachments: ... })` ← 收到的是 undefined → 存为 null

**结果**: TaskContext.attachments = null → Kelly 拿不到文件 → 输出假设性内容。

> 此问题已在 Issue 0 的修改方案中覆盖。

---

## 执行顺序建议

| 步骤 | 优先级 | 涉及文件 | 预计影响 |
|------|--------|---------|---------|
| **Step 1** | 🔴 P0 | `file-parser.ts` | 验证并修复 PDF/DOCX/XLSX 解析 — 不解析等于白干 |
| **Step 2** | 🔴 P0 | `office/page.tsx` | 修复全自动流程附件传递断链 |
| **Step 3** | 🟡 P1 | `agent_capabilities.yaml` + `orchestrate/route.ts` + `analyze/route.ts` | Kelly 多文件拆分并行 |
| **Step 4** | 🟢 P2 | `history/page.tsx` | 历史页面展示附件列表 |

## 验证计划

### 自动验证
```bash
# 创建验证脚本，对每种文件格式进行单元测试
node scripts/test-file-parser.js  # 测试 PDF、DOCX、XLSX、TXT 解析
```

### 手动验证 (localhost:5859)
1. 上传 4 个不同格式文件 (xlsx + pdf + docx + md)
2. 全自动执行 → 检查：
   - Chief 是否为每个文件创建独立 Kelly 任务
   - 每个 Kelly 是否输出真实文件内容（非假设）
   - 管线日志中是否显示附件已关联
3. 查看 `/history` 页面 → 确认附件列表正常显示

## Open Questions

> [!IMPORTANT]
> **关于 pdf-parse v2 API**: 需要先运行验证脚本确认 `pdf-parse@2.4.5` 的实际导出格式。v1 和 v2 的 API 差异较大，当前代码的 `PDFParse` 类写法可能是错的。

> [!NOTE]
> **关于文件大小限制**: 目前 `extractedText` 注入 prompt 时有 3000 字符限制 ([bristh-config.ts L107](file:///Users/aisandbox/Documents/myAI/src/lib/bristh-config.ts#L107-L108))。对于大文件（比如那个 246KB 的 PDF），这可能不够。是否需要提升限制或改用分块处理？
